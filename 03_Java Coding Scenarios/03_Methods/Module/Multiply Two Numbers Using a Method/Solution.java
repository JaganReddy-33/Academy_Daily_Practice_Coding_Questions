class Main {
    public static void main(String[] args) {
        // Predefined input values
        int a = 5; // You can change this value for testing
        int b = 4; // You can change this value for testing

        // Call the multiplication method and store the result
        int result = multiplyNumbers(a, b);

        // Print the result
        System.out.println(result);
    }

    // Define the method to multiply two integers and return the product
    public static int multiplyNumbers(int x, int y) {
        return x * y;
    }
}