# HTML MCQ: Single-Line Text Input Element

> ✅ Solved

## Question

<p>What type of form element is used to create a single-line text input in HTML?</p>

## Answer choices

- ⬜ **A.** <p>There is no tag in HTML. Use <code>&lt;input&gt;</code> tag with type="text" for single-line text input.</p>
- ⬜ **B.** <p>There is no tag in HTML for creating single-line text input. Use <code>&lt;input&gt;</code> tag with type="text" instead.</p>
- ✅ **C. <p>The <code>&lt;input&gt;</code> tag with type="text" attribute creates a single-line text input in HTML.</p>** — Correct answer
- ⬜ **D.** <p>There is no tag in HTML. Use <code>&lt;input&gt;</code> tag with type="text" for single-line text input.</p>

## Submission

- **Correct answer:** <p>The <code>&lt;input&gt;</code> tag with type="text" attribute creates a single-line text input in HTML.</p>
- **Submitted at:** 2026-05-28T08:03:39.091Z
- **Correct submission:** True

## Explanation

_Not available._
